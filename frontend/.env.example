# Copy to .env.local to override. Both settings have working defaults.

# Set to "false" to point the service layer at the real API instead of mocks.
VITE_USE_MOCKS=true

# API prefix. Left as /api, the Vite dev server proxies to FastAPI on :8080.
VITE_API_BASE_URL=/api
